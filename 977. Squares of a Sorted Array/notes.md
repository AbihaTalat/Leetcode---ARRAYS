## Application:

simple multiplication of indexes of arrays. Pushback function to store the array and then Sort function in which resides to more functions, begin() and end() which takes the beginning and end of the array and swap. Not the end of the array, one past the end.

## Learning Points:
Sorting an array using a sort() function. 

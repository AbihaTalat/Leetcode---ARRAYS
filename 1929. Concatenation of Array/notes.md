## Application:

I tried the approach of indexing. i + nums.size() stores the same values in the new indexes and ans[i] = nums[i] just stores the values in the new arrays as it is.

## Learning Points:
Understood the difference between indexing and use of push_back function, when and where to use. Also how to give size to the a vector.

## Application:

Here the approach is, that I made a vector variable first to store the output and then i initialized a variable and named it as “sum” which stores the sum of the array numbers at each iteration. I applied for loop so it iterates for each number in the array and gets added with the previous number/sum of the array. Since we don’t know the exact size of the array s we used .size(). and then we used .push_back() to store each sum. As we the sum ends and the loop ends, we return v which is the array that stores the sum at each iteration.

## Where I went wrong:
I manually entered values in the nums array and gave a restricted size in the for loop. Honestly had to relearn alot about vectors.

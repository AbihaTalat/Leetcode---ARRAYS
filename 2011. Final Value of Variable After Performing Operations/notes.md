## Application:

We initialized a variable. Only one was allowed. In the if block, the condition means in simple terms if the current index of array contains a “+” so do the operation X++ ,else do X- - .  Th e condition “operations[i][1]” can be understood by the following example: if the loop runs and i = 0 → "++X” then "++X"[1] and then gives that a “+”.

##Learning Point:
It was this part of the code "operations[i][1]". This means the first index of operations and whatever string it contains, in that string whatever character exists on index [1] is equal to + or not.
